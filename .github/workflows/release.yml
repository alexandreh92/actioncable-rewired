name: Release

on:
  pull_request:
    branches:
      - main

jobs:
  create-version:
    name: Bump new version
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true

    steps:
      - name: Lerna version
        run: |
          echo Bumping new release
